-- รันไฟล์นี้ใน SQL Editor ถ้าฐานข้อมูลของคุณมีตาราง batches อยู่แล้ว
-- และคอลัมน์ servings ยังเป็น int อยู่
--
-- ทำไมต้องรัน: ฟีเจอร์ "กำหนดขนาดขวดเอง" ตอนนี้รองรับกรณีที่ขนาดขวดน้อยกว่า
-- 1 เสิร์ฟเต็ม (เช่น สูตรต่อเสิร์ฟ 500 มล. แต่กำหนดขวด 300 มล.) โดยจะปรับสัดส่วน
-- ส่วนผสมลงมาให้พอดี ทำให้ค่า servings อาจเป็นเลขทศนิยม เช่น 0.6 ซึ่งเก็บในคอลัมน์
-- ชนิด int ไม่ได้ ต้องเปลี่ยนเป็น numeric ก่อน

alter table batches alter column servings type numeric using servings::numeric;
